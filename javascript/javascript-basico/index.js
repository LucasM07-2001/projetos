let produto = "Monitor"
let mensagemProduto = `O produto ${produto.toUpperCase()} esta com desconto em nossas lojas`

console.log(mensagemProduto)

document.getElementById("mensagem").textContent = mensagemProduto